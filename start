#!/bin/bash

ffmpeg \
  -i `youtube-dl -f best -g "$VIDEO_URL"` \
  -rtbufsize 100M \
  -maxrate 2M \
  -bufsize 1M \
  -b:v 2M \
  -pix_fmt yuv420p \
  -c:v libx264 \
  -c:a aac \
  -preset ultrafast \
  -f flv "$RTMP_URL"

