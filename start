#!/bin/bash

ffmpeg \
  -i `youtube-dl -f best -g "$VIDEO_URL"` \
  -pix_fmt yuv420p \
  -c:v libx264 \
  -c:a aac \
  -rtbufsize 100M \
  -maxrate 2M \
  -b:v 2M \
  -preset ultrafast \
  -f flv "$RTMP_URL"

